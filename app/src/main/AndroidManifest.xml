<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quizzit"
        tools:targetApi="31">

        <!-- Declaración de CreateBlock Activity -->
        <activity
            android:name=".CreateBlock"
            android:exported="true" />
        <activity
            android:name=".ExamModeActivity"
            android:exported="true" />

        <!-- Declaración de QuestionActivity -->
        <activity
            android:name=".QuestionActivity"
            android:exported="true" />

        <!-- Declaración de otras actividades -->
        <activity
            android:name=".PracticeModeActivity"
            android:exported="true" />

        <!-- Declaración principal (MainActivity) -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Quizzit"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>